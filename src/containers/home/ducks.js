export const HOME_LOADING_START = 'home/loading_start';


const initialState = {
  loading: true,
};

export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
}
